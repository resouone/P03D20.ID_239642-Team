#include "parser.h"

int to_rpn(const Token *tokens, int token_count, Token *output, int max_output) {
    (void)tokens;
    (void)token_count;
    (void)output;
    (void)max_output;

    return 1; // фиктивный rpn
}
